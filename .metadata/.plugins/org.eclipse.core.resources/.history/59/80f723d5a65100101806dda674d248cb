#include "iostream"
#include "ros/ros.h"
#include "webots/Robot.h"

int main(int argc, char** argv)
{
  ros::init(argc, argv, "demo_supervisor");
  ros::NodeHandle n;

  while(n.ok())
  {
    ros::spinOnce();
  }
  return 0;
}
